# Ultralytics YOLO 🚀 AGPL-3.0 License - https://ultralytics.com/license
# YOLOv26 双流检测模型配置文件
# 支持RGB + IR双模态输入

# Parameters
nc: 80  # 类别数
scales:  # 模型复杂度和准确性比例因子
  # [depth, width, max_channels]
  n: [0.33, 0.25, 1024]  # YOLOv26n
  s: [0.33, 0.50, 1024]  # YOLOv26s
  m: [0.67, 0.75, 768]   # YOLOv26m
  l: [1.00, 1.00, 512]   # YOLOv26l
  x: [1.00, 1.25, 512]   # YOLOv26x

# YOLOv26 双流backbone
backbone:
  # [from, repeats, module, args]

  ########## RGB Stream ##########
  # RGB 输入处理流
  - [-1, 1, Conv, [64, 3, 2]]  # 0-P1/2 RGB stream start
  - [-1, 1, Conv, [128, 3, 2]]  # 1-P2/4
  - [-1, 3, C2f, [128, True]]   # 2
  - [-1, 1, Conv, [256, 3, 2]]  # 3-P3/8
  - [-1, 6, C2f, [256, True]]   # 4

  ########## IR Stream ##########
  # IR 输入处理流（从6通道输入的第4-6通道开始）
  - [-6, 1, Conv, [64, 3, 2]]   # 5-P1/2 IR stream start (从最初输入分离)
  - [-1, 1, Conv, [128, 3, 2]]  # 6-P2/4
  - [-1, 3, C2f, [128, True]]   # 7
  - [-1, 1, Conv, [256, 3, 2]]  # 8-P3/8
  - [-1, 6, C2f, [256, True]]   # 9

  ########## Fusion Layer 1 ##########
  # 第一次特征融合 (P3/8 level)
  - [[4, 9], 1, DualStreamFusion, [256, "concat"]]  # 10 融合RGB和IR的P3特征

  ########## RGB Continue ##########
  - [4, 1, Conv, [512, 3, 2]]   # 11-P4/16 RGB continue
  - [-1, 6, C2f, [512, True]]   # 12
  - [-1, 1, Conv, [1024, 3, 2]] # 13-P5/32
  - [-1, 3, C2f, [1024, True]]  # 14
  - [-1, 1, SPPF, [1024, 5]]    # 15

  ########## IR Continue ##########
  - [9, 1, Conv, [512, 3, 2]]   # 16-P4/16 IR continue
  - [-1, 6, C2f, [512, True]]   # 17
  - [-1, 1, Conv, [1024, 3, 2]] # 18-P5/32
  - [-1, 3, C2f, [1024, True]]  # 19
  - [-1, 1, SPPF, [1024, 5]]    # 20

  ########## Fusion Layer 2 ##########
  # 第二次特征融合 (P4/16 level)
  - [[12, 17], 1, DualStreamFusion, [512, "concat"]]  # 21 融合RGB和IR的P4特征

  ########## Fusion Layer 3 ##########
  # 第三次特征融合 (P5/32 level)
  - [[15, 20], 1, DualStreamFusion, [1024, "concat"]]  # 22 融合RGB和IR的P5特征

# YOLOv26 双流head
head:
  # 使用融合后的特征进行检测
  - [-1, 1, nn.Upsample, [None, 2, "nearest"]]  # 23
  - [[-1, 21], 1, Concat, [1]]  # 24 cat P4 fusion
  - [-1, 3, C2f, [512]]  # 25

  - [-1, 1, nn.Upsample, [None, 2, "nearest"]]  # 26
  - [[-1, 10], 1, Concat, [1]]  # 27 cat P3 fusion
  - [-1, 3, C2f, [256]]  # 28 (P3/8-small)

  - [-1, 1, Conv, [256, 3, 2]]  # 29
  - [[-1, 25], 1, Concat, [1]]  # 30 cat head P4
  - [-1, 3, C2f, [512]]  # 31 (P4/16-medium)

  - [-1, 1, Conv, [512, 3, 2]]  # 32
  - [[-1, 22], 1, Concat, [1]]  # 33 cat head P5
  - [-1, 3, C2f, [1024]]  # 34 (P5/32-large)

  - [[28, 31, 34], 1, Detect, [nc]]  # 35 Detect(P3, P4, P5)